// Muss mit next.config.ts's `basePath` übereinstimmen (GitHub-Pages-Unterordner).
// Wird manuell vor rohe "/..."-Asset-Pfade in <img src> gesetzt, da basePath/assetPrefix
// nur von Next.js selbst erzeugte Asset-URLs automatisch anpassen, nicht eigene <img>-Tags.
export const basePath = "/RehbeinPortfolio";
